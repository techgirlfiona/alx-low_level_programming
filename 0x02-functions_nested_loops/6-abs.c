#include "main.h"
/**
 * _abs - compute the absolute value of an integer
 * @n: AN integre input
 * int - abs
 * Return: absolute value of number
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);

}
